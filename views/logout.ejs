
<div class="page-header">
	<div class="row">
		<div class="col-md-6 text-left">
			<a href="/profile" class="btn btn-default btn-sm btn-primary">Profile</a>
			<% if(sumDebt && sumDebt.length > 0 ) { %>
				<%= sumDebt[0].sumDebt %>
			<% } else { %> 0 <% } %>
		</div>
		<div class="col-md-6 text-right">
			<a href="/logout" class="btn btn-default btn-sm btn-danger">
			<% if(username) { %>
				<%= username %>
			<% } %>
			Logout</a>
		</div>
	</div>
</div>
